const isValid = (brackets) => {
    let 
  let obj = {
    "{": "}",
    "(": ")",
    "[": "]",
    };
    
    for (let i = 0; i < brackets.length; i++){

    }

};
let brackets = "{}()";
console.log(isValid)(brackets);
