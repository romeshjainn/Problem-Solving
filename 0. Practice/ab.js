let arr = [2, 3, 4, 1, 6, 7, 8];
num = 2;
let halfLength = arr.length / 2;
halfLength = Math.ceil(halfLength);
// console.log(halfLength);

function check(arr, num) {
  for (let i = halfLength - 1; i < arr.length; i++) {
    
  }
}
check(arr, num);
